name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.event_name == 'pull_request' && format('ci-pr-{0}', github.ref) || format('cicd-{0}', github.ref) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2

  test:
    name: Test with coverage gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build and test cmd (no business logic, no coverage floor)
        run: go test -race ./cmd/...

      - name: Run tests (unit, integration, chaos, property; Docker available on the runner)
        # -p 2 caps how many packages spin testcontainers at once, so a small
        # runner is not starting Postgres and toxiproxy for several packages in
        # parallel and racing on container startup.
        run: go test -race -p 2 ./internal/... -covermode=atomic -coverprofile=cover.out

      - name: Enforce coverage gate
        run: COVER_PROFILE=cover.out make cover

      - uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: cover.out

  smoke-load:
    name: Smoke load test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Boot the load-test stack
        run: docker compose --profile load-test up -d --build

      - name: Wait for health
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8080/healthz; then
              exit 0
            fi
            sleep 2
          done
          echo "app never became healthy"
          docker compose --profile load-test logs app
          exit 1

      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update && sudo apt-get install -y k6

      - name: Smoke load (gate on errors and a loose p99 ceiling only)
        run: SMOKE=1 k6 run test/load/post_transactions.js

      - name: Teardown
        if: always()
        run: docker compose --profile load-test down

  coverage-badge:
    name: Publish coverage badge
    needs: [test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/download-artifact@v4
        with:
          name: coverage

      - name: Build shields endpoint JSON
        run: |
          # Match scripts/coverage.sh: strip generated packages (0% coverage,
          # no tests of their own) before computing the total, so the badge
          # reports the same number the coverage gate enforces, not a number
          # dragged down by generated code.
          EXCLUDE='internal/genproto/|internal/postgres/sqlc/'
          FILTERED="$(mktemp)"
          head -n 1 cover.out > "$FILTERED"
          grep -vE "$EXCLUDE" cover.out | tail -n +2 >> "$FILTERED" || true
          PCT=$(go tool cover -func="$FILTERED" | grep '^total:' | awk '{print $3}' | tr -d '%')
          rm -f "$FILTERED"
          COLOR=red
          awk "BEGIN{exit !($PCT>=70 && $PCT<80)}" && COLOR=yellow
          awk "BEGIN{exit !($PCT>=80)}" && COLOR=brightgreen
          mkdir -p out
          printf '{"schemaVersion":1,"label":"coverage","message":"%s%%","color":"%s"}\n' "$PCT" "$COLOR" > out/coverage.json

      - name: Publish to badges branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp out/coverage.json /tmp/coverage.json
          git fetch origin badges || true
          if git checkout badges 2>/dev/null; then
            find . -mindepth 1 -maxdepth 1 -not -name .git -exec rm -rf {} +
          else
            git checkout --orphan badges
            git rm -rf . >/dev/null 2>&1 || true
          fi
          cp /tmp/coverage.json coverage.json
          git add coverage.json
          git commit -m "chore: update coverage badge" || echo "no change"
          git push origin badges

  deploy:
    name: Deploy to VPS
    needs: [lint, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build linux/amd64 binary
        # -X main.buildRevision stamps the short commit SHA into the binary
        # (Task 5.6a, audit A6.1) so /healthz and the build_info metric report
        # exactly which build is serving in production (Task 5.6b makes the
        # deployed binary actually carry it, not just the field existing).
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath \
            -ldflags="-s -w -X main.buildRevision=${GITHUB_SHA::7}" \
            -o go-ledger ./cmd/server

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          printf '%s\n' "${{ secrets.DEPLOY_KNOWN_HOSTS }}" > ~/.ssh/known_hosts

      - name: Upload binary
        run: scp -i ~/.ssh/deploy_key go-ledger "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/opt/go-ledger/go-ledger.new"

      - name: Migrate (pre-swap)
        # Applies every pending goose migration against production BEFORE the
        # new binary is swapped in (Task 5.6b, audit A4.3, ADR-017 "Migrations
        # run in CI before deploy"): schema changes land ahead of the code
        # that needs them, and a failed migration fails this step, which
        # stops the job here, so the swap-and-restart step below never runs
        # and the currently running (old) binary is left untouched. The
        # go-ledger-migrate wrapper (infra/ansible/roles/app) loads the box's
        # own /etc/go-ledger/env and execs go-ledger.new migrate; the script
        # itself is root-owned (so `deploy` cannot tamper with it) and
        # reachable only through the narrowly scoped sudoers rule installed
        # by that role, but that rule's Runas_Spec is `(goledger)`, not
        # `(root)`: sudo runs the wrapper, and go-ledger.new migrate under
        # it, as the sandboxed goledger app user, never as root. That caps a
        # leaked DEPLOY_SSH_KEY at "run arbitrary code as the app user"
        # (same ceiling the running service already has), not root RCE,
        # even though go-ledger.new itself lives under deploy's own
        # ownership and is overwritten by this same pipeline every push.
        # Best-effort: if the box has not yet been provisioned with the
        # NOPASSWD sudoers rule + wrapper (the ansible app role installs them,
        # and this pipeline does not run ansible), `sudo -n` fails fast without
        # running anything. In that case we warn and continue, because the app
        # applies migrations on boot (runMigrations in run()) when systemd
        # restarts it below (single-instance, ADR-013). A genuine migration
        # failure (the wrapper ran and goose errored) still aborts the deploy so
        # the swap never happens. Provision the box (run the ansible app role)
        # to get true pre-swap migration.
        run: |
          ssh -i ~/.ssh/deploy_key "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" '
            set -euo pipefail
            chmod 755 /opt/go-ledger/go-ledger.new
            if out=$(sudo -n -u goledger /usr/local/sbin/go-ledger-migrate 2>&1); then
              printf "%s\n" "$out"
              echo "pre-swap migration applied"
            else
              printf "%s\n" "$out"
              if printf "%s" "$out" | grep -qiE "password is required|terminal is required|not allowed|command not found|no such file"; then
                echo "::warning::pre-swap migrate wrapper not provisioned on the box; relying on the app on-boot migration. Run the ansible app role to enable pre-swap migration."
              else
                echo "::error::pre-swap migration failed"
                exit 1
              fi
            fi
          '

      - name: Swap binary and restart service
        run: |
          ssh -i ~/.ssh/deploy_key "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" '
            set -euo pipefail
            cd /opt/go-ledger
            if [ -f go-ledger ]; then mv go-ledger go-ledger.prev; fi
            mv go-ledger.new go-ledger
            sudo systemctl restart go-ledger
          '

      - name: Health check (rollback on failure)
        # A bad binary self-heals to the last-known-good one (Task 5.6b,
        # audit A6.2): if the post-restart health check fails, ssh back in,
        # restore go-ledger.prev over go-ledger, restart, and re-check health,
        # then fail the pipeline loudly either way. A rollback does NOT
        # un-apply the migration that just ran; see docs/ops/server-setup.md
        # for why migrations must stay backward-compatible with the previous
        # binary for one release.
        run: |
          sleep 3
          # A6.2: confirm the box is not just up (HTTP 200) but actually serving
          # THIS build. /healthz reports the build revision stamped at build
          # time (-X main.buildRevision=${GITHUB_SHA::7}); a silent binary-swap
          # or restart failure that left the OLD binary running would still
          # return 200 and pass a status-only check, so compare the served
          # revision to what this pipeline just deployed and roll back on a
          # mismatch too, not only on a non-200. Extraction is dependency-free
          # (no jq): the body is {"status":"ok","revision":"<sha7>"}.
          EXPECTED_REV="${GITHUB_SHA::7}"
          HEALTH_BODY="$(curl --fail --silent --show-error --retry 5 --retry-delay 3 --retry-all-errors https://go.sohag.pro/healthz || true)"
          SERVED_REV="$(printf '%s' "$HEALTH_BODY" | sed -n 's/.*"revision":"\([^"]*\)".*/\1/p')"
          if [ -n "$HEALTH_BODY" ] && [ "$SERVED_REV" = "$EXPECTED_REV" ]; then
            echo "health OK: serving revision ${SERVED_REV}"
            exit 0
          fi

          echo "::error::health check failed after deploy: served revision '${SERVED_REV:-none}' does not match the deployed '${EXPECTED_REV}' (or the app did not return 200), so the binary swap or restart did not take effect; rolling back to the previous binary"
          # This step's shell runs with -e (the GitHub Actions run: default),
          # so a naive `ssh ... || exit 1` right after it would still let -e
          # kill the script the instant the ssh command below returns
          # non-zero (e.g. no go-ledger.prev, or the restart itself failing),
          # skipping every diagnostic below and leaving the log silent about
          # which failure actually happened. Guarding the ssh call as the
          # condition of an `if` neutralizes -e for just this command, so the
          # explicit ::error:: message and exit 1 always run instead.
          if ! ssh -i ~/.ssh/deploy_key "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" '
            set -euo pipefail
            cd /opt/go-ledger
            if [ ! -f go-ledger.prev ]; then
              echo "no go-ledger.prev on the box to roll back to" >&2
              exit 1
            fi
            cp go-ledger.prev go-ledger
            sudo systemctl restart go-ledger
          '; then
            echo "::error::rollback itself failed (no go-ledger.prev on the box, or the restart command failed); the app is likely still down on the bad build, investigate immediately"
            exit 1
          fi

          sleep 3
          if curl --fail --silent --show-error --retry 5 --retry-delay 3 --retry-all-errors https://go.sohag.pro/healthz; then
            echo "::error::rolled back to the previous binary after a failed health check; the new build did NOT go live, investigate before retrying the deploy"
          else
            echo "::error::rollback restart also failed its health check; the app may be down, investigate immediately"
          fi
          exit 1

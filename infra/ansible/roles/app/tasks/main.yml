---
- name: Create the app system user (no home, no login)
  ansible.builtin.user:
    name: "{{ app_user }}"
    system: true
    create_home: false
    shell: /usr/sbin/nologin

- name: Create the deploy user (key-only, receives CI deploys)
  ansible.builtin.user:
    name: "{{ deploy_user }}"
    create_home: true
    shell: /bin/bash

- name: App directory owned by the deploy user
  ansible.builtin.file:
    path: "{{ app_dir }}"
    state: directory
    owner: "{{ deploy_user }}"
    group: "{{ deploy_user }}"
    mode: "0755"

- name: Env config directory
  ansible.builtin.file:
    path: "{{ env_file | dirname }}"
    state: directory
    mode: "0755"

- name: Root-owned env file, readable by the app group
  ansible.builtin.copy:
    dest: "{{ env_file }}"
    content: "PORT=8080\n"
    owner: root
    group: "{{ app_user }}"
    mode: "0640"
    force: false

- name: Install the pre-swap migrate wrapper (Task 5.6b, audit A4.3)
  ansible.builtin.template:
    src: go-ledger-migrate.j2
    dest: /usr/local/sbin/go-ledger-migrate
    owner: root
    group: root
    mode: "0755"

- name: Restricted sudoers so deploy can restart the app as root and run the migrate wrapper as the app user, not root
  ansible.builtin.template:
    src: sudoers.j2
    dest: /etc/sudoers.d/go-ledger-deploy
    mode: "0440"
    validate: "visudo -cf %s"

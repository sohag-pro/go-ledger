version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - misspell
    - revive
    - unconvert
    - unparam
    - gosec
    - copyloopvar

formatters:
  enable:
    - gofumpt
    - goimports
